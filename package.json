{
  "name": "gatsby-cypress-test",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "start": "yarn workspace static-site run unsafe:develop",
    "open": "yarn workspace cypress cypress open"
  },
  "dependencies": {
    "npm-run-all": "4.1.5",
    "start-server-and-test": "^1.7.11",
    "yarn": "1.13.0"
  },
  "devDependencies": {
    "babel-plugin-lodash": "^3.3.4"
  }
}
